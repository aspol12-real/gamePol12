#include "ppu.hpp"

void ppu::tick() {
    switch (ppuState) {
        case OAMSearch:

            break;

        case PixelTransfer:
            break;
        
        case HBlank:
            break;

        case VBlank:
            break;
    }
}